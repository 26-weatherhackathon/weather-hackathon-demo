// grade.ts — 점수→등급 매핑 (Build Spec §3). 스캐폴드 스텁.
export type Grade = 'rookie' | 'junior' | 'senior' | 'expert' | 'chief';

export function gradeFromScore(_total: number): Grade {
  // TODO: 점수 구간 → 등급 배지 매핑.
  return 'rookie';
}
