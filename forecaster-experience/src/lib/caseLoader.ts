// caseLoader.ts — 케이스 JSON 로더 (Build Spec §3). 스캐폴드 스텁.
import type { CaseMeta } from './types';

export async function loadCaseMeta(caseId: string): Promise<CaseMeta | null> {
  // TODO: public/data/cases/<caseId>/meta.json 로드.
  void caseId;
  return null;
}
