import type { FunctionComponent } from 'preact';

// ForecastInput — Preact island (Build Spec §3). 스캐폴드 스텁; 추후 일정에서 구현.
const ForecastInput: FunctionComponent = () => {
  return <div data-stub="ForecastInput" />;
};

export default ForecastInput;
