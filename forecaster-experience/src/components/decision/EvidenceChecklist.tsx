import type { FunctionComponent } from 'preact';

// EvidenceChecklist — Preact island (Build Spec §3). 스캐폴드 스텁; 추후 일정에서 구현.
const EvidenceChecklist: FunctionComponent = () => {
  return <div data-stub="EvidenceChecklist" />;
};

export default EvidenceChecklist;
