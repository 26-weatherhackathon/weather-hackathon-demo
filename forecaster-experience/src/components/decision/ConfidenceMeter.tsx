import type { FunctionComponent } from 'preact';

// ConfidenceMeter — Preact island (Build Spec §3). 스캐폴드 스텁; 추후 일정에서 구현.
const ConfidenceMeter: FunctionComponent = () => {
  return <div data-stub="ConfidenceMeter" />;
};

export default ConfidenceMeter;
