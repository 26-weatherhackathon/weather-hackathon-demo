import type { FunctionComponent } from 'preact';

// ConceptQuiz — Preact island (Build Spec §3). 스캐폴드 스텁; 추후 일정에서 구현.
const ConceptQuiz: FunctionComponent = () => {
  return <div data-stub="ConceptQuiz" />;
};

export default ConceptQuiz;
