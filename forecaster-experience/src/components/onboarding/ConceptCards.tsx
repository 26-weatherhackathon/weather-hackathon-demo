import type { FunctionComponent } from 'preact';

// ConceptCards — Preact island (Build Spec §3). 스캐폴드 스텁; 추후 일정에서 구현.
const ConceptCards: FunctionComponent = () => {
  return <div data-stub="ConceptCards" />;
};

export default ConceptCards;
