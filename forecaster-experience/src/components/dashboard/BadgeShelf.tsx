import type { FunctionComponent } from 'preact';

// BadgeShelf — Preact island (Build Spec §3). 스캐폴드 스텁; 추후 일정에서 구현.
const BadgeShelf: FunctionComponent = () => {
  return <div data-stub="BadgeShelf" />;
};

export default BadgeShelf;
