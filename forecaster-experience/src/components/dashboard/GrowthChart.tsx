import type { FunctionComponent } from 'preact';

// GrowthChart — Preact island (Build Spec §3). 스캐폴드 스텁; 추후 일정에서 구현.
const GrowthChart: FunctionComponent = () => {
  return <div data-stub="GrowthChart" />;
};

export default GrowthChart;
