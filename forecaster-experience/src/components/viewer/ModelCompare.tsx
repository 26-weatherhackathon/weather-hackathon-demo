import type { FunctionComponent } from 'preact';

// ModelCompare — Preact island (Build Spec §3). 스캐폴드 스텁; 추후 일정에서 구현.
const ModelCompare: FunctionComponent = () => {
  return <div data-stub="ModelCompare" />;
};

export default ModelCompare;
