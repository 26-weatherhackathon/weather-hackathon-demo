import type { FunctionComponent } from 'preact';

// LayerViewer — Preact island (Build Spec §3). 스캐폴드 스텁; 추후 일정에서 구현.
const LayerViewer: FunctionComponent = () => {
  return <div data-stub="LayerViewer" />;
};

export default LayerViewer;
