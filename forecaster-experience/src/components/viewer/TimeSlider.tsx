import type { FunctionComponent } from 'preact';

// TimeSlider — Preact island (Build Spec §3). 스캐폴드 스텁; 추후 일정에서 구현.
const TimeSlider: FunctionComponent = () => {
  return <div data-stub="TimeSlider" />;
};

export default TimeSlider;
