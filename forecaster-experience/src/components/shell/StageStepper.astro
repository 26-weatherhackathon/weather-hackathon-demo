---
// StageStepper — 5단계 진행 표시 (Build Spec §3, §5-C C5). 스캐폴드 스텁.
interface Props { current?: number; }
const { current = 0 } = Astro.props;
---
<nav data-stub="StageStepper" data-current={current}></nav>
