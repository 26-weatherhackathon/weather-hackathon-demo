import type { FunctionComponent } from 'preact';

// ForecasterAvatar — Preact island (Build Spec §3). 스캐폴드 스텁; 추후 일정에서 구현.
const ForecasterAvatar: FunctionComponent = () => {
  return <div data-stub="ForecasterAvatar" />;
};

export default ForecasterAvatar;
