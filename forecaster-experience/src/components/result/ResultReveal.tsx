import type { FunctionComponent } from 'preact';

// ResultReveal — Preact island (Build Spec §3). 스캐폴드 스텁; 추후 일정에서 구현.
const ResultReveal: FunctionComponent = () => {
  return <div data-stub="ResultReveal" />;
};

export default ResultReveal;
