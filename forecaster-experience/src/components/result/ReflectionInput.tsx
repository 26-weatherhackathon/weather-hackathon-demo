import type { FunctionComponent } from 'preact';

// ReflectionInput — Preact island (Build Spec §3). 스캐폴드 스텁; 추후 일정에서 구현.
const ReflectionInput: FunctionComponent = () => {
  return <div data-stub="ReflectionInput" />;
};

export default ReflectionInput;
