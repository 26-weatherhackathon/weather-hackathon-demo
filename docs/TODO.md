# TODO

`docs/PLAN.md`와 실제 구현(PR #24, `feat: 아이소메트릭 렌더링 엔진 + 방재 배치 게임 루프`) 사이에 남아 있는 정합성·개선 과제. 변경 이력 자체는 git 커밋/PR 히스토리로 추적하고, 이 파일은 아직 끝나지 않은 작업만 담는다.

### Todo

- [ ] 기상 데이터 UI 노출 순화 (CLAUDE.md 1항 위반) — 현재 화면이 "기상청 API 연결됨" 상태와 "강수확률 95% · 강수량 48.5mm" 원시 수치를 그대로 노출한다. CLAUDE.md 1항("API 사용 자체를 학습자에게 과시하는 UI로 노출하지 않는다")과 PLAN.md 4.1(2)의 "비가 많이 와요" 초등 눈높이 표현 원칙에 맞춰 순화한다.
- [ ] 인명 보호(대피·피난) 메커닉 결정 — PLAN.md 초안의 "대피소·모두 피난"(사람이 먼저다)이 구현에서 빠지고 건물 침수 여부만 다룬다. 학습 목표에 인명 안전을 되살릴지, 건물 방어로 범위를 확정할지 결정하고 PLAN.md에 반영한다.
- [ ] 물 흐름 방향 시각화 검토 — 초안의 D8 물 흐름 화살표가 게임 루프에 없다. "물이 어디로 흐르는가" 학습 요소를 되살릴지, 현재의 수위 상승 모델로 대체 확정할지 검토한다.
- [ ] 기상 API 실배선 — `weather.ts`는 3초 타임아웃 Mock 폴백까지 구현되었으나, 시나리오 수위(`peakLevel` 등)를 실데이터로 구동하는 서버 Route Handler 배선이 남아 있다.
- [ ] `README.md`/`LICENSES.md` 작성 (CLAUDE.md 5항·3항) — 아직 작성되지 않았다.

### In Progress

### Done
